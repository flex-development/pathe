/**
 * @file Unit Tests - dot
 * @module pathe/lib/tests/unit/dot
 */

import TEST_SUBJECT from '../dot'

describe('unit:lib/dot', () => {
  it('should equal "."', () => {
    expect(TEST_SUBJECT).to.eq('.')
  })
})
